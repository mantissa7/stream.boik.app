# Loaded automatically by `vite build` (production mode).
# Backend (livestream lookup + Twitch chat auth) is served from boik.app.
# stream.boik.app and boik.app are the same site, so the .boik.app session
# cookie set by the backend is sent on these cross-origin requests.
VITE_STREAM_API_BASE=https://boik.app
